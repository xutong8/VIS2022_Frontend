const NODE_NAME_CARD = "card";

enum NodeType {
  D = "D",
  V = "V",
}

enum ChartType {
  SCATTER = "scatter",
  LINE = "line",
  CAT_LINE = "cat_line",
}

export { NODE_NAME_CARD, NodeType, ChartType };
